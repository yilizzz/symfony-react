import React from 'react';

const Hello = () => {
    return (
        <div style={{ padding: '20px', backgroundColor: '#f0f0f0', borderRadius: '8px' }}>
            <h1>你好，Symfony + React! 🚀</h1>
            <p>这是一个运行在 WSL 里的 React 组件。</p>
        </div>
    );
};

export default Hello;